# 작업 규칙

- 작업 중 의미 있는 변경이 누적되면 수시로 커밋한다.
- 커밋 직후 원격 저장소의 현재 작업 브랜치로 푸시한다.
- 커밋과 푸시를 수행할 때마다 사용자에게 커밋 해시, 변경 요약, 푸시 결과를 알린다.
- 푸시 전에 `git status`와 현재 브랜치 및 원격 저장소를 확인하고, 인증 오류나 충돌이 발생하면 임의로 우회하지 않고 사용자에게 알린다.
