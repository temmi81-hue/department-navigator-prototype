# 전사 사규·기준 AI 네비게이터: 구현 가이드

## 결론: 처음부터 "AI 규정 해석 서비스"를 만들지 않는다

과제제안서의 핵심은 사용자가 업무 상황을 자연어로 입력하면 **관련 규정의 근거를 보여 주고, 필요한 유관 부서를 안내·연결**하는 것이다. 이 데모에서는 `260827_조직 및 책임권한 규정_업무분장_더미파일.docx`를 **업무분장의 기준 원본**으로 사용한다. 실제 사규 본문과 공식 검토 채널은 아직 연결되어 있지 않으므로, 데모의 범위는 부서 안내로 제한한다.

따라서 첫 목표는 다음 한 가지로 줄인다.

> 사용자가 상황과 사업장을 입력하면, 업무분장 더미파일에서 추출한 데이터를 근거로 **후보 부서와 이유**를 보여 주는 파일 기반 데모를 완성한다.

이 데모가 안정화된 뒤에만 규정 검색(RAG), 검토 요청, SSO/ECM 연동을 추가한다. 이 순서는 과제제안서의 "맞춤 해석·유관부서 자동연계" 방향을 유지하면서도, 잘못된 규정 안내나 자동 의사결정의 위험을 피한다.

---

## 현재 상태 진단

### 참고 자료에서 확인한 목표

- 과제제안서: 수작업 규정 검색과 개별 문의를 줄이고, 상황 기반 안내와 유관부서 연계를 제공한다. 예시 업무는 설비 교체, 신규 원료 구매, 투자·공사 등이다.
- 업무분장 더미파일: 조직별 책임업무와 사업장 단서를 갖고 있어, **이 데모의 부서 후보 추천 기준 데이터**로 쓴다.
- `pilot_departments.json`: 위 더미파일에서 실제 조직명과 책임업무를 대조해 추린 15개 부서만 담은 데모 지식베이스다. 현재 웹서비스는 이 파일만 읽는다.
- `initial_department_knowledge_base.json`: 기존 추출본은 참고 자료로만 둔다. 웹서비스 추천에 직접 연결하지 않는다.

### 현재 `PRD.md`와 프로토타입의 문제

| 항목 | 확인한 상태 | 먼저 할 일 |
| --- | --- | --- |
| 한글 인코딩 | `PRD.md`, JSON, 화면 코드에 한글 깨짐(모지바케)이 있다. | 원본에서 UTF-8로 다시 저장하고, 깨진 파일은 내용 복구 후 교체한다. |
| 프런트엔드 코드 | `app/page.tsx`에 닫히지 않은 문자열·태그가 있어 컴파일되지 않는 상태로 보인다. | 기능 추가보다 먼저 TypeScript/JSX 문법 오류를 제거한다. |
| 실행 도구 | 이 작업 환경에서는 `pnpm` 명령을 찾지 못했다. | Node.js와 Corepack/pnpm을 준비한 뒤 `pnpm install`, `pnpm run build`를 실행한다. |
| 데이터 신뢰도 | 업무분장 더미파일을 데모 기준으로 사용한다. 다만 문서의 목차 안내문이나 빈 책임업무가 추출 데이터에 섞일 수 있다. | 더미파일에서 선택한 항목만 정제하고, 결과에 `데모 데이터`를 표시한다. 자동 배정은 하지 않는다. |
| AI 범위 | 현재 화면은 키워드 점수 기반 부서 추천만 하며, 규정 본문 검색·인용은 없다. | 1차에는 이를 정직하게 "부서 찾기"로 제공하고 AI 해석을 약속하지 않는다. |

---

## 권장 서비스 범위

### 1차 데모(MVP): 부서 찾기

사용자가 입력하는 값은 세 개면 충분하다.

1. 업무 상황: 예) "포항 공장의 설비를 교체하려는데 먼저 협의할 부서를 알고 싶어요."
2. 사업장: 포항/광양/세종/전사/미입력
3. 업무 유형: 안전, 환경, 설비·정비, 구매·자재, 투자·공사, 생산·조업 중 선택 또는 자동 추정

보여 주는 결과는 다음으로 제한한다.

- 주관 후보 1곳과 협업 후보 최대 3곳
- 주관 후보와 협업 후보 각각의 `주요 업무` 요약(각 부서의 JSON 책임업무)
- `추천 근거 보기` 화면에서 주관 후보의 추천 이유, 책임업무, 원문 위치를 별도로 확인
- `업무분장 더미파일 기반 데모` 안내
- 협업 후보는 안내용으로 표시하고, 체크한 후보만 주관 후보와 함께 메일 초안 수신 부서에 포함
- "검토 요청 내용 준비" → "관련 부서에 검토 요청을 할까요?" → 예/아니오 확인
- 예를 누르면 일반 협조 요청 형식의 수정 가능한 메일 초안과 복사·메일 앱 열기 버튼을 제공한다. 실제 전송은 하지 않는다.

**1차에서 제외할 것**: SSO, ECM 연동, 실제 규정 해석, 법적/안전 최종 판단, 자동 배정, 실시간 알림, 관리자 콘솔. 이것들은 데이터 소유자·권한·업무 채널이 확정된 뒤의 2차 범위다.

### 2차: 규정 근거 안내

공식 사규 문서와 열람 권한이 준비되면 추가한다.

- 문서를 조항 단위로 분할하고 제목, 버전, 시행일, 적용 사업장, 권한 정보를 붙인다.
- 질문마다 허용된 문서만 검색하고, 답변에는 문서명·조항·버전·원문 링크를 반드시 붙인다.
- 인용 근거가 없으면 "확인할 근거를 찾지 못했습니다"라고 안내하고 검토 요청으로 전환한다.
- 안전·환경·법무·투자전결 같은 고위험 주제는 답변보다 검토 요청 경로를 우선한다.

### 3차: 공식 업무 연계

검토 요청 채널, SLA, 담당자, 승인 권한이 확정된 경우에만 실제 티켓 생성과 상태 조회를 연결한다. 추천 결과와 실제 처리 결과를 저장해 추천 규칙을 개선하되, AI가 조직 책임을 자동으로 변경해서는 안 된다.

---

## 데이터 준비: 개발보다 먼저 할 일

업무분장 더미파일 전체를 브라우저에서 직접 검색하지 말고, 원문에서 조직명과 책임업무가 함께 확인되는 15개 부서를 골라 `pilot_departments.json`을 만든다. 현재 데모에는 안전·환경·컴플라이언스·구매·투자·공사·현장 정비·품질을 대표하는 15개를 넣었다. 이 파일의 원본은 반드시 업무분장 더미파일이며, 별도 조직 승인 절차는 데모의 선행 조건이 아니다.

### 데모에 선정한 15개 부서

1. 안전보건기획그룹
2. 안전QSS섹션
3. 환경자원기획그룹
4. 정도경영그룹
5. 투자관리그룹
6. 원료구매1그룹
7. 원료구매2그룹
8. 설비자재구매그룹
9. 기초소재품질섹션
10. 포항안전환경그룹
11. 광양안전환경그룹
12. 플랜트공사그룹
13. 플랜트안전섹션
14. 포항양극재정비섹션
15. 광양양극재정비섹션

`pilot_departments.json`의 각 항목은 `id`, `name`, `parent`, `sites`, `categories`, `keywords`, `responsibility`, `sourcePath`를 사용한다. `name`과 `sourcePath`는 더미파일 원문에 실제 등장하는 명칭만 허용한다.

데모 데이터 정제 체크리스트:

- 조직명·상위 조직명·사업장이 최신인가?
- 책임업무 문구가 실제 담당 범위를 설명하는가?
- 과제제안서의 대표 시나리오(설비 교체, 신규 원료 구매, 투자·공사)와 연결되는가?
- 빈 책임업무·목차 안내문·중복 조직명이 제거됐는가?
- 원문 위치와 추출일이 남아 있는가?

### Stitch 사용 시 반드시 지킬 데이터 규칙

Stitch는 화면 시안을 만드는 도구이지, 업무분장 사실을 판정하는 도구가 아니다. 화면을 생성할 때 다음 문장을 요구사항에 그대로 포함한다.

```text
부서명과 책임업무를 새로 만들지 말 것.
추천 후보는 public/pilot_departments.json의 organizations 배열에 있는 15개 항목에서만 선택할 것.
배열에 없는 부서명, 담당자, 연락처, SLA, 규정 조항, 원문 인용을 추측하거나 생성하지 말 것.
일치하는 조직이 없으면 '업무분장 더미파일에서 확인되는 부서를 찾지 못했습니다'라고 표시할 것.
화면의 모든 부서 카드에는 해당 항목의 name, responsibility, sourcePath를 그대로 출력할 것.
```

Stitch가 생성한 화면은 반드시 다음을 다시 확인한다.

- 부서명은 아래 15개 목록 중 하나인가?
- 책임업무 문구와 원문 위치는 JSON 값에서 그대로 가져오는가?
- JSON에 없는 새 부서 카드나 가상의 담당자 정보가 없는가?
- 검색 결과가 없을 때 임의의 부서를 추천하지 않는가?

화면 디자인을 Stitch로 만들더라도 추천 로직은 현재 `page.tsx`의 규칙 기반 코드와 `pilot_departments.json`을 소스 오브 트루스로 유지한다. Stitch가 만든 텍스트를 데이터 파일이나 추천 로직에 복사해 넣지 않는다.

---

## 구현 순서

### 0. 실행 가능한 빈 화면부터 복구

1. 모든 소스와 JSON을 UTF-8로 저장한다.
2. `app/page.tsx`를 짧은 정상 화면으로 교체한다. 첫 화면에는 제목, 업무 입력칸, 사업장 선택, 결과 영역만 둔다.
3. `pilot_departments.json`을 `public/`에 두고 브라우저에서 불러온다.
4. 아래 명령이 성공할 때까지 기능을 늘리지 않는다.

```powershell
$env:Path = 'C:\Program Files\nodejs;' + $env:Path
& 'C:\Program Files\nodejs\corepack.cmd' pnpm install
& 'C:\Program Files\nodejs\corepack.cmd' pnpm run build
& 'C:\Program Files\nodejs\corepack.cmd' pnpm run dev
```

이 PC에는 Node.js와 Corepack이 설치되어 있지만 기본 터미널 PATH에 Node.js가 빠져 있을 수 있다. 위 첫 줄은 현재 PowerShell 창에서만 경로를 보완한다. `pnpm install` 중 빌드 스크립트 승인 화면이 나오면 `pnpm approve-builds`로 프로젝트 의존성만 승인한 뒤 다시 실행한다.

### 1. 결정적(규칙 기반) 추천 구현

AI 대신 설명 가능한 점수 규칙을 먼저 만든다.

| 조건 | 예시 점수 | 화면에 보일 이유 |
| --- | ---: | --- |
| 업무유형 일치 | +6 | `설비·정비 책임업무와 일치` |
| 사업장 일치 | +5 | `포항 사업장 담당` |
| 키워드 일치 | 키워드당 +2 | `설비`, `교체` 키워드 일치 |
| 더미파일에 명시된 책임업무와 직접 일치 | +4 | `업무분장 원문 책임업무와 일치` |
| 빈 책임업무·목차 문구 | 결과 제외 | 표시하지 않음 |

동점이거나 사업장이 빠진 경우에는 주관 부서를 단정하지 않는다. "사업장을 선택해 주세요" 또는 복수 후보를 보여 주는 것이 올바른 동작이다.

### 2. 검토 요청은 '준비'까지만

버튼을 누르면 다음 내용을 사용자가 확인·수정할 수 있는 텍스트로 만든다.

```text
제목: [검토 요청] 설비 교체 사전 협의
업무 상황: 포항 공장의 설비 교체 예정
추천 부서: 포항 설비정비 섹션 (업무분장 더미파일 기준)
추천 근거: 설비·정비 업무유형 및 포항 사업장 일치
확인 요청: 사전 협의 절차와 추가 검토 부서를 확인 부탁드립니다.
```

실제 메일·티켓 전송은 조직별 채널과 수신자, SLA를 승인받은 뒤에만 붙인다.

### 3. 규정 검색은 별도 기능으로 추가

규정 검색은 부서 추천과 분리한다. "부서 찾기"가 안정화된 뒤 다음 순서로 만든다.

1. 공식 문서와 열람 권한을 확정한다.
2. 문서를 조항 단위로 정제하고 버전·시행일·권한 메타데이터를 부여한다.
3. 검색 결과에 없는 내용은 생성하지 않도록 답변 정책을 둔다.
4. 20개 표준 질문으로 인용 정확도·최신성·권한 누출을 시험한다.
5. 통과한 주제만 순차 공개한다.

---

## 화면 설계(최소)

```text
[규정 길잡이]
무엇을 하려는지 알려 주세요.

[업무 상황 입력 ______________________________]
[사업장: 선택하세요 v] [찾기]

관련 부서 후보
  [주관 후보] 포항양극재정비섹션        신뢰도: 높음
  설비·정비 책임업무 + 포항 사업장 일치
  근거: 업무분장 > 에너지소재정비실 > 포항에너지소재정비부 > ...
  [검토 요청 내용 준비]

  [협업 후보] 안전/환경 담당 조직
  주요 업무: 업무분장 JSON의 책임업무 요약
  (체크하면 주관 후보와 함께 요청 메일 초안에 포함됩니다.)

  [추천 근거 보기] → 추천 이유·책임업무·원문 위치를 별도 화면에 표시

  [검토 요청 내용 준비] → [관련 부서에 검토 요청을 할까요?]
  → 예: 메일 초안 생성 / 아니오: 요청 종료

※ 이 결과는 업무분장 더미파일에 따른 데모 안내이며 최종 판단이 아닙니다.
```

질문 답변을 그럴듯하게 생성하는 채팅 화면보다, 추천 근거·데이터 상태·다음 행동을 명확히 보이는 화면이 1차 성공에 더 중요하다.

---

## 완료 기준과 테스트

### 빌드 완료 기준

- `pnpm run lint`와 `pnpm run build`가 오류 없이 끝난다.
- 한글이 화면, JSON, 콘솔에서 깨지지 않는다.
- 네트워크 오류 시 "데이터를 불러오지 못했습니다"라는 안내가 표시된다.

### 기능 테스트 6개

| 입력 | 기대 결과 |
| --- | --- |
| 포항 설비 교체 | 포항 + 설비·정비가 일치한 더미파일 조직이 상단에 나온다. |
| 광양 신규 원료 구매 | 광양 + 구매·자재 후보가 상단에 나온다. |
| 사업장 없는 설비 교체 | 사업장 추가 입력을 요구하거나 복수 후보를 보여 준다. |
| 빈 책임업무 또는 목차 문구 | 검색 결과에 노출되지 않는다. |
| 관련 키워드가 없는 질문 | 임의 부서를 주관으로 단정하지 않고 추가 정보를 요청한다. |
| 협업 후보 체크 | 체크한 후보만 메일 초안의 협업 요청 부서와 수신 부서에 포함된다. |
| 추천 근거 보기 | 메일 본문과 분리된 화면에 추천 이유·책임업무·원문 위치를 표시한다. |
| 검토 요청 버튼 | 예/아니오 확인 뒤, 외부 전송 없이 수정 가능한 요청 초안만 만든다. |

각 테스트에는 입력, 화면 캡처, 실제 결과, 통과/실패를 기록한다. 과제제안서의 정량 목표(규정 확인 1시간→10분, 부서 응답 대기 1~3일→즉시)는 이 데모의 성능 목표가 아니라, 운영 데이터가 쌓인 뒤 측정할 사업 효과 지표로 둔다.

---

## 다음 행동

가장 작은 성공 단위는 다음 네 가지다.

1. 깨진 인코딩과 문법 오류를 고쳐 빈 화면을 빌드한다.
2. 위 15개 조직의 원문 위치·책임업무를 대조한 `pilot_departments.json`을 기준 데이터로 사용한다.
3. 설비 교체·신규 원료 구매·투자/공사 등 대표 질문 6개가 설명 가능한 규칙으로 통과하게 만든다.
4. 결과를 담당 부서에 보여 주고, 추천·근거·요청 문구를 보정한다.

이 네 가지가 끝나면 비로소 규정 원문 검색과 AI 답변을 추가할 준비가 된다.

---

## 독립 HTML 데모 실행

React 화면과 별도로, Stitch 화면을 확인하면서 데이터 연결을 검증할 수 있는
`department-navigator-prototype/public/demo.html`을 제공한다. 이 페이지는
`./pilot_departments.json`만 읽어 추천하므로 반드시 로컬 서버로 열어야 한다.

```powershell
cd C:\Users\user\Desktop\연습\department-navigator-prototype
$env:Path = 'C:\Program Files\nodejs;' + $env:Path
& 'C:\Program Files\nodejs\corepack.cmd' pnpm run dev -- --host 127.0.0.1 --port 3000
```

브라우저에서 `http://localhost:3000/demo.html`을 연다. `demo.html`을 파일 탐색기에서
직접 더블클릭(`file://`)하면 브라우저 보안 정책상 JSON을 읽지 못할 수 있다.

### 바로 시험할 질문

아래 질문은 화면의 빠른 테스트 버튼으로도 제공한다. 괄호는 데이터에 따라 우선 노출될
후보 예시이며, 실제 결과는 `pilot_departments.json`의 15개 조직 안에서만 결정된다.

| 질문 | 사업장 | 확인할 후보 |
| --- | --- | --- |
| 포항 공장의 설비를 교체하려는데 먼저 협의할 부서를 알고 싶어요. | 포항 | 포항양극재정비섹션, 포항안전환경그룹 |
| 신규 원료 구매 전에 품질과 환경 측면에서 확인할 부서를 찾아주세요. | 미선택 | 원료구매1그룹, 원료구매2그룹, 기초소재품질섹션, 환경자원기획그룹 |
| 공장 증설 공사를 준비하는데 투자와 안전 측면에서 협의가 필요합니다. | 광양 | 투자관리그룹, 플랜트공사그룹, 플랜트안전섹션 |
| 수의계약을 진행하기 전에 감사나 컴플라이언스 검토가 필요한지 알고 싶어요. | 전사 | 정도경영그룹 |

추천 카드의 `근거 보기`에는 JSON의 책임업무와 `sourcePath`가 표시되고, `검토 요청 내용
준비`는 외부로 전송하지 않는 수정 가능한 초안만 만든다. 따라서 Stitch가 임의로 만든
부서명은 실제 추천 로직에 들어오지 않으며, 화면 문구와 데이터 로직을 반드시 분리해 검증한다.
